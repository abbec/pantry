from setuptools import setup
setup(name='pantry',
        version='0.1',
        packages=['pantry'],
        install_requires=[
            'Flask',
            'Flask-SQLAlchemy',
            ]
        )
